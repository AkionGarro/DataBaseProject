create or replace PACKAGE PACKAGEPCD is
procedure new_APPUSER(p_username in VARCHAR2, 
                             p_idNumber in VARCHAR2,
                             p_userType in NUMBER, 
                             p_district in NUMBER,
		 	     p_password in VARCHAR2,
		             p_phoneNumber in NUMBER,
			     p_Email in VARCHAR2);
procedure new_COUNTRY(p_nameCountry in VARCHAR2);
PROCEDURE new_HISTORY_APPUSERXBUYSALE(p_idUser in VARCHAR2, 
                                              p_idSale in NUMBER);
PROCEDURE new_MSG_USERXUSER(p_idWriter in varchar2,
                                        p_idRecipient in varchar2,
                                        p_subject in VARCHAR2,
                                        descritpionM IN VARCHAR2, sendDate in DATE); 
procedure new_People(p_identification in VARCHAR2, 
                             p_idType in NUMBER,
                             p_idGender in NUMBER, 
                             p_idCountry in NUMBER,
		 	     p_birthdate in DATE,
		             p_name in VARCHAR2,
			     p_surname in VARCHAR2,
			     p_secondsurname VARCHAR2);
procedure new_shcart_appuserxclock(p_username in VARCHAR2,
				      p_Clock in NUMBER);
procedure new_Brand(p_nameBrand in VARCHAR2);
PROCEDURE new_BUYSTATUS(p_status in VARCHAR2, p_descriptionB in varchar2);
procedure new_BUYERRATING(
                        p_idBuyRating in VARCHAR2, 
                        p_username in VARCHAR2,
                        p_rating in VARCHAR2);
procedure new_buySale(p_idUserS in VARCHAR2, 
                             p_idC in NUMBER,
                             p_idBuyS in NUMBER, 
                             p_date in DATE);
procedure new_CITY(p_nameCity in VARCHAR2,
				      p_Countryid in NUMBER);
procedure new_CLOCK(
                            p_idClock in NUMBER,
                            p_idType in NUMBER, 
                            p_idModel in NUMBER,
		 	                p_idCondition in NUMBER,
		                    p_descriptionClock in VARCHAR2,
			                p_manufactureDate in DATE,
                            p_vintage in VARCHAR2,
                            p_condition in VARCHAR2,
                            p_price in VARCHAR2);
procedure new_Condition(p_nameCondition in VARCHAR2,
                                          p_description in VARCHAR2);
procedure new_DISTRICT(
                        p_idDistrict in NUMBER, 
                        p_idCity in NUMBER ,
                        p_nameDistrict in VARCHAR2);
procedure new_GENDER(p_nameGender in VARCHAR2);
procedure new_IDENTIFICATION(p_identification in VARCHAR2);
procedure new_MODELP(
                        p_idModel in NUMBER, 
                        p_idBrand in NUMBER,
                        p_nameModel in VARCHAR2);
procedure new_PAYMENTMETHOD(
                        p_idPay in NUMBER, 
                        p_type in VARCHAR2);
PROCEDURE new_PHOTOCLOCK(p_idClock in NUMBER, 
                                           p_photo in BLOB,
                                           p_descriptionP in varchar2);
PROCEDURE new_Review(p_description in VARCHAR2, rating in NUMBER);
procedure new_SELLERRATING(
                        p_idSellRating in VARCHAR2, 
                        p_username in VARCHAR2,
                        p_rating in VARCHAR2);
procedure  new_typeClock(p_nameType in VARCHAR2,
                                           p_description in VARCHAR2 );
procedure new_USERTYPE(
                        p_idUserType in NUMBER, 
                        p_descriptionType in VARCHAR2);
procedure new_WLISTUSERXCLOCK(
                        p_idUser in VARCHAR2, 
                        p_idClock in NUMBER);
END PACKAGEPCD;